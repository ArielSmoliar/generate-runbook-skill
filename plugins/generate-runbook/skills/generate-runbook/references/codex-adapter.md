# Codex adapter

- Use commentary updates while inspecting or executing a long runbook.
- Use available read-only tools to resolve facts before asking the user.
- Follow workspace sandbox and approval requirements.
- Use `apply_patch` for file edits.
- Link local runbooks with absolute Markdown paths in the final response.
- Never claim an external mutation, deployment, message, or test succeeded without tool evidence or user confirmation.
- If execution pauses at an approval gate, present the exact next action, target, impact, verification, and rollback.
